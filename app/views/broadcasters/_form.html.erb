<%= form_with(model: broadcaster, local: true) do |form| %>
  <% if broadcaster.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(broadcaster.errors.count, "error") %> prohibited this broadcaster from being saved:</h2>

      <ul>
      <% broadcaster.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :broadcast_key %>
    <%= form.text_field :broadcast_key %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
